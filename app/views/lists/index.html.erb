<h3> These are your todo lists </h3>

<ul>
<% @list.each do |list| %>
  <li>
  <%= link_to list.name, specific_list_path(list) %>
  </li>
<% end %>
</ul>


<%= button_to "Create New List", new_list_path, method: "get", class: "btn btn-default" %>